
// Objeto com propriedades e valores.
const person = {
    name: 'Clayton',
    age: 35,
    weigth: 104.0,
    isAdmin: true
}

// Mostrando o valor de uma unica propriedade 
console.log(person.name)

// Usando Template string para deixai o conteudo mais dinamico(ex: `${}`)
console.log(`${person.name} tem ${person.age} anos`)

