/*
    * Variáveis

        * a palavra reservada (var) é de uso global e, também local
        * a palavra reservada (let) é de uso local, só funciona no escopo pnde foi criada

        3 palavras reservadas para criar uma variavel

            - var
            - let
            - const / o valor não pode ser alterado 

    ## conhecendo o tipo da variavel

        - ex: let clima = "" / string
              let clima = 0 / number
              let clima = true / false / boolean

            console.log(typeof clima) = i tipo da variavel sera imprimido.


    ## Interpolando valores com template string

        let name = "clayton"
        let age = 35

        ex: console.log(`o ${name} tem ${age} anos`)

*/